# 历史记录好感度分析
## 指标
好感度（总）、态度、情感共振率、聊天积极度、喜好

## 具体操作
注：标蓝的参数为用户可设定

### 预处理
- 各个聊天对象的有序消息列表（按时间排序）；
- 对每条消息的情感分析（包括情感极性（积极1、中性0、消极-1）、强度分数（从-1（极度消极）到1（极度积极），中性接近0）、句向量）；
- 根据话题延续性划分会话：用sentence-transformers库生成句向量，滑动窗口（窗口的消息条数可自己测试调整），记录所有相似度分数形成序列，找到“谷值”（如果相似度 < 阈值则标记为话题边界，阈值可测试调整），此外可以结合时间间隔辅助，如果时间间隔过高，即使相似度高也强制分割（可以根据用户给出的聊天对象标签不同有所不同）；
- 关键词列表准备（包括正面词汇、负面词汇、专属称呼、隐私分享关键词（如：“秘密”）、节假日列表、共情关键词、安抚关键词、喜好关键词（需要用户提供喜好）等）；
- 用户给出聊天对象的标签（例如是同事还是好朋友）；
- 聊天对象高频回复时段；

### 情感共振率维度（权重0.3）
#### 前提处理：构建交互对
遍历每个会话内部的消息（记住要是一个会话内部的），连续的消息合并为一个“发言单位”（间隔小于五分钟，时间间隔过大就不合并），从一方的一个发言单元，到另一方的下一个发言单元，形成一对（双向都算）

#### 参数（加实现方法）（权重）
- 双向积极情感响应率（积极消息互发数 / 总积极消息数）（20%）
  - 参数：
    - 总积极消息数（遍历所有会话，计数极性=1的消息（双方总和））；
    - 积极消息互发数（遍历每个会话内的交互对，如果一方发积极消息（极性=1），另一方回复也积极（极性=1），计+1（双向都算：我积极→对方积极，或对方积极→我积极），累加所有会话的互发数。）
- 情感极性一致性得分（双方同极性消息占比 × 语义相似度均值）（15%）
  - 参数：
    - 同极性消息占比（遍历所有会话内的交互对，如果一对中双方极性相同（都1、都0、都-1），计为一致，同极性对数 / 总交互对数 = 同极性占比（如果无交互对，设为0）。）
    - 语义相似度均值（只针对同极性交互对，对每对，用句向量计算余弦相似度（预存向量），取这些对的相似度平均值（如果无同极性对，设为0）。）
- 情绪强度匹配度（双方单条消息情感得分差值的绝对值均值的倒数）（10%）
  - 参数：
    - 差值绝对值列表（遍历所有会话内的交互对，对每对计算 |强度分数A - 强度分数B|，收集所有差值的绝对值列表。）
  - 处理：
    - 如果无交互对，设为0，mean_abs_diff = 所有绝对差的平均值，匹配度 = 1 / (mean_abs_diff + 0.1) （加0.1防除0），如果值>1，用 1 - tanh(mean_abs_diff) 夹到0-1。
- 共情意图识别率（包含共情关键词的消息数 / 总互动消息数）（30%）
  - 参数：
    - 共情消息数（遍历会话统计包含共情关键词的消息数）（后续优化可以增加在回复我的情绪消息后加分，数据更准）；
    - 总互动消息数；
- 负面情绪协同化解率（化解回复 / 负面发起对）（25%）
  - 参数；
    - 负面发起对（遍历所有交互对，如果发起方是“我”，且发起极性=-1（消极），计为一个负面发起对）；
    - 化解回复（检查每个负面发起对，检查回复方的极性和是否包含安抚关键词，如果都满足则计为一次化解回复）；

### 聊天积极度维度（权重0.3）
#### 参数（加实现方法）（权重）
- 日均聊天消息数（总消息数/聊天天数）（10%）
- 回复及时率（20%）
  - 公式=在规定时效内回复的轮次数/需统计的轮次数*100%
  - 参数：
    - 规定时效（如五分钟、一小时、一天等，在这个时间段内回复才算及时回复）；
    - 及时回复的对话轮次（将连续的消息合并成一个轮次，在规定时效内回复则为及时回复）；
    - 未及时回复的对话轮次；
    - 需要统计的总轮次数（及时回复的对话轮次+未及时回复的对话轮次）。
- 单条消息平均长度（10%）
- 长文本（超过100字）占比（15%）
- 话题延续性得分（基于语义相似度）（20%）
  - 参数：时窗（让用户根据聊天对象习性进行设置，话题延续性得分会根据时窗来划分聊天记录们进行延续性判断，保底为一天）
  - 根据时窗划分预处理阶段已经得出的会话，向上取整（就是只要这个会话包含时窗内的消息，就把这个会话算进来），分别计算每个会话内部的话题延续性得分，然后取平均分，再得出时窗内的话题延续性平均分，最后得出记录总体的话题延续性得分
- 主动发起聊天次数占比（25%）
  - 公式=对方主动发起聊天次数/总发起聊天次数（两轮对话之间的时间要大于冷却时间）
  - 参数：
    - 对方主动发起聊天次数（详细算法见下方）；
    - 本人主动发起聊天次数；
    - 总发起聊天次数（对方主动发起聊天次数+本人主动发起聊天次数）；
  - 主动发起聊天判断算法：
   1. 核心参数绑定
| 参数名称 | 取值规则 |
|----------|----------|
| 兜底冷却时间 | 用户设定的规定时效 |
| 相似度阈值 | 0.4（默认值，用户可改） |

   2. 判定逻辑（两步走，无歧义）
| 相邻消息间隔 | 判定步骤 | 最终结论 |
|--------------|----------|----------|
| ≤ 规定时效（冷却时间） | 直接判定 → 同一话题 | 被动回复，不计入主动发起次数 |
| ＞ 规定时效（冷却时间） | 计算余弦相似度：<br>1. 相似度 ≥ 0.4 → 同一话题<br>2. 相似度 ＜ 0.4 → 新话题 | 1. 被动回复<br>2. 主动发起，次数 + 1 |
| 聊天第一条消息 | - | 主动发起，次数 + 1 |


### 态度倾向维度（权重0.2）
#### 前提准备：总消息数（分母常用）
- 正面词汇出现频次（总正面词汇出现消息数/总消息数）（如 "哈哈"" 谢谢 ""想你"）（25%）
- 负面词汇出现频次（总负面词汇出现消息数/总消息数）（反向扣分20%）
- 表情包 / 语音 / 视频通话使用占比（总表情包/语音/视频通话出现消息数/总消息数）（10%）
  - 三个互动类型的权重为：0.3：0.2：0.5
- 专属称呼使用频率（昵称、外号）（总专属称呼出现消息数/总消息数）（25%）
- 隐私内容分享比例（总隐私内容分享关键词汇出现消息数/总消息数）（20%）
- 节假日祝福发送情况（总节假日祝福出现消息数/总消息数）（10%）

### 喜好维度（权重0.2）
- 话题提及频率（提及喜好的会话次数/总会话数）（40%）
  - 参数：
    - 提及喜好的会话次数（遍历所有会话，统计出现用户喜好关键词的会话次数）；
    - 总会话数；
- 喜好话题延续性得分（基于语义相似度，判断是否与你喜好相同）（60%）
  - 遍历所有提及喜好的会话，计算该会话内的延续性得分，最后的出所有提及喜好的会话的平均延续性得分