# 基础依赖
pywebview>=4.0.0
pywin32>=305
jieba>=0.42.1
numpy>=1.24.0

# 微信数据库解密依赖
pycryptodome>=3.20.0  # 纯 Python 实现的 SQLCipher 4 解密

# 好感度分析依赖 (002-affinity-analysis)
snownlp>=0.12.3
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
torch>=2.0.0
pytest>=7.0.0

# 实时消息监听依赖
# wxauto4>=40.1.1
# 注意：wxauto4 在 PyPI 不可用，需手动安装
# 安装方法：pip install "下载的zip文件路径"
# 下载地址：https://github.com/AngeCoo/wxauto-4.0/archive/refs/heads/main.zip