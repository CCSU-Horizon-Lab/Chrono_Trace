# 基础依赖
pywebview>=4.0.0
pywin32>=305
pywinauto>=0.6.8
Pillow>=10.0.0
jieba>=0.42.1
numpy>=1.24.0

# 微信数据库解密依赖
pycryptodome>=3.20.0  # 纯 Python 实现的 SQLCipher 4 解密

# 好感度分析依赖 (002-affinity-analysis)
snownlp>=0.12.3
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
torch>=2.0.0
pytest>=7.0.0
transformers>=4.30.0  # 实时消息情感分析

# 实时消息监听依赖
# 实时监听统一使用 pywinauto + native_uia
# wxauto 已不再作为项目依赖或运行时兜底
